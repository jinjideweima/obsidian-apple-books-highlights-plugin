# 运行 `npm run deploy` / `npm run build:deploy` 时，把 dist/ 里的插件文件
# 复制到这个目录。复制本文件为 .env.local，并改成你自己 vault 的插件目录：
OBSIDIAN_PLUGIN_DIR="/absolute/path/to/your-vault/.obsidian/plugins/apple-books-knowledge-cards"
